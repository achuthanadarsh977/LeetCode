function addDigits(num: number): number {

    let sum = 0
    if(num === 0){
        return 0
    }

    if(num < 10){
        return num
    }


    while(num > 0){
        sum = sum + (num % 10)
        num = Math.floor(num/10)

    }

    return addDigits(sum)
    
};
