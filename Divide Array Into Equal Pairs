function divideArray(nums: number[]): boolean {

    const n  = new Map<number,number>()

    for(const r of nums){
        n.set(r , (n.get(r) || 0) + 1)
    }

    for(const i of nums){
        if(n.get(i) % 2 === 1){
            return false
        }
    }
    return true
};
