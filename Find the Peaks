function findPeaks(mountain: number[]): number[] {

    let m = mountain.length
    let res:number[] = []

    for(let i=1;i<m;i++){
        if(mountain[i]>mountain[i-1] && mountain[i] > mountain[i+1]){
            res.push(i)
        }
    }

    return res
    
};
