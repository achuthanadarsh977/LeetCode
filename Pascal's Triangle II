function getRow(rowIndex: number): number[] {

    let row = [1]

    for(let i=1;i<=rowIndex;i++){
        let next_element = (row[i-1] * (rowIndex - i + 1))/i
        row.push(next_element)
    }
    
    return row


    
};
